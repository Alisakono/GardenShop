spring.application.name=GardenShop
#MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/garden
spring.datasource.username=root
spring.datasource.password=Allesklar123!

spring.jpa.hibernate.ddl-auto=none
spring.jpa.generate-ddl=false

# --- Liquibase ------
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/db/db.changelog-master.xml

#Log SQL queries
spring.jpa.showSql=true
spring.jpa.properties.hibernate.format_sql=true
#logging.level.com.telran.gardenshop=DEBUG

logging.config=src/main/resources/db/log4j2.xml

# JSON serialization settings
spring.jackson.serialization.fail-on-empty-beans=true

#XML-Json format for REST config http://web.app?format=json
#spring.mvc.contentnegotiation.favor-parameter=true

#logging.level.root=info

# Pagination settings
spring.data.web.pageable.max-page-size=50

